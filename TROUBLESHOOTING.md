# Troubleshooting

```bash
ls ~/.claude/plugins/ | grep -c '^libre-copy-'  # should print 20
```

Common scenarios:
- API doc design → `/api-docs`
- README that nobody reads → `/readme-engineering` (v0.3)
- Runbook for an incident → `/runbook-writing` (v0.3)
- Tutorial that loses people → `/tutorial-creation` (v0.3)
- Error message UX → `/error-messages` (v0.4)
